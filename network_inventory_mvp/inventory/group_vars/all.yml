---
# Read-only collection over NETCONF.
ansible_connection: ansible.netcommon.netconf
ansible_network_os: juniper.device.junos
ansible_port: 830

# Recommended: provide credentials at runtime or via Ansible Vault.
# Do NOT commit production passwords here.
# ansible_user: network_ro
# ansible_password: "{{ vault_junos_password }}"
